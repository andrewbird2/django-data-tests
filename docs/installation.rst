============
Installation
============

At the command line::

    $ easy_install django-data-tests

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-data-tests
    $ pip install django-data-tests
